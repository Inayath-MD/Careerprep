# Roadmap Features Checklist

- [x] Implement `findByUserOrderByCreatedAtDesc` in `InterviewSessionRepository`
- [x] Implement `getHistory()` and `getDashboard()` in `ResumeService`
- [x] Add `/resume/history` and `/resume/dashboard` in `ResumeController`
- [x] Create `roadmap.py` in Python FastAPI backend
- [x] Expose `POST /generate-roadmap` in `app.py`
- [x] Verify execution of history and roadmap APIs
